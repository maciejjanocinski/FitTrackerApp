spring:
  datasource:
    url: jdbc:mysql://localhost:3306/FitTrackerApp
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
  show-sql: true
logging:
  level:
    org.springframework: DEBUG
    com.example: DEBUG